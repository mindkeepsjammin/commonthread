# Things to Build

1. **Home Dashboard** — Currently all placeholder cards. Wire it to show real relationships, recent reflections, and family activity to make the app feel alive.

2. **Alder Wyn AI Chat** — UI exists but message sending is stubbed. Needs AI backend integration (the DB table is ready).

3. **Relationship Detail Page** — Uses hardcoded data. Needs real queries, health score display, and interaction.

4. **Reflection Sharing** — DB supports it, but there's no UI to select family members to share with.

5. **Relational Hearts / Health Scoring** — Schema exists but no calculation logic or visualization.

6. **Account Deletion** — Only signs out locally; server-side deletion is a TODO (important for compliance).

7. **Common Threads Discovery** — The signature feature (finding patterns in shared reflections) has no implementation yet.
